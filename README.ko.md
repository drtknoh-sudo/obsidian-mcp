# obsidian-mcp

[English](README.md)

AI 어시스턴트가 Obsidian 볼트를 **읽고 쓸 수 있게** 해주는 빠른 MCP 서버입니다.

## ✨ 주요 기능

| 기능 | 설명 |
|------|------|
| 📖 **노트 읽기** | 볼트의 모든 노트 내용 읽기 |
| ✏️ **노트 생성** | 마크다운으로 새 노트 만들기 |
| 📝 **노트 수정** | 기존 노트 내용 변경 |
| 🗑️ **노트 삭제** | 안전하게 휴지통으로 이동 |
| 🔍 **제목 검색** | 파일명으로 노트 찾기 |
| 📄 **전문 검색** | 노트 내용에서 검색 |
| 📁 **폴더 탐색** | 볼트 구조 확인 |
| 🏷️ **태그 조회** | 사용 중인 모든 태그 목록 |
| 📅 **날짜 자동 입력** | 생성일/수정일 자동 기록 |

## 🆕 날짜 자동 입력 기능 (v0.2.0)

새 노트를 만들면 YAML frontmatter가 자동으로 추가됩니다:

```yaml
---
created: 2026-01-29
modified: 2026-01-29
tags: []
---
```

- **create_note**: `created`와 `modified` 날짜 자동 생성
- **update_note**: 수정 시 `modified` 날짜 자동 업데이트
- `auto_frontmatter: false`로 비활성화 가능

## 🚀 왜 만들었나요?

| | obsidian-mcp | 클라우드 API |
|---|:---:|:---:|
| 노트 읽기 | ✅ | ✅ |
| **노트 쓰기** | ✅ | ❌ |
| 속도 | ⚡ 즉시 | 🐌 네트워크 지연 |
| 오프라인 | ✅ | ❌ |
| 사용 제한 | 없음 | 있음 |
| API 키 | 불필요 | 필요 |
| 개인정보 | 100% 로컬 | 클라우드 동기화 |

### ⚡ 성능 (추정치)

| 작업 | 2만 개 노트 기준 |
|------|-----------------|
| 노트 목록 | **0.17초** |
| 전체 내용 읽기 | 0.63초 |
| 전문 검색 | 0.66초 |

## 📋 필요한 것

- Python 3.10 이상
- [Obsidian](https://obsidian.md/) 설치됨
- [uv](https://github.com/astral-sh/uv) 패키지 매니저 (권장)

## 📦 설치 방법

### 1단계: 이 저장소 다운로드

```bash
git clone https://github.com/drtknoh-sudo/obsidian-mcp.git
cd obsidian-mcp
```

### 2단계: 의존성 설치

```bash
uv sync
```

### 3단계: Claude Desktop 설정

Claude Desktop 설정 파일을 엽니다:

**macOS:**
```bash
open ~/Library/Application\ Support/Claude/claude_desktop_config.json
```

**Windows:**
```
%APPDATA%\Claude\claude_desktop_config.json
```

아래 내용을 추가합니다:

```json
{
  "mcpServers": {
    "obsidian": {
      "command": "/uv경로",
      "args": ["run", "--directory", "/obsidian-mcp경로", "obsidian-mcp"],
      "env": {
        "OBSIDIAN_VAULT_PATH": "/볼트경로"
      }
    }
  }
}
```

**중요:** 경로를 본인 환경에 맞게 수정하세요:
- `/uv경로` → 터미널에서 `which uv` 실행하여 확인 (예: `/Users/사용자명/.local/bin/uv`)
- `/obsidian-mcp경로` → 이 저장소를 다운받은 위치
- `/볼트경로` → Obsidian 볼트 폴더 위치

### 4단계: Claude Desktop 재시작

Claude Desktop을 완전히 종료(Cmd+Q)하고 다시 실행합니다.

## 🛠️ 사용 가능한 도구

| 도구 | 설명 |
|------|------|
| `list_notes` | 노트 목록 (폴더 필터 가능) |
| `get_note` | 특정 노트 내용 읽기 |
| `create_note` | 새 노트 생성 (날짜 자동 추가) |
| `update_note` | 노트 수정 (수정일 자동 업데이트) |
| `delete_note` | 노트를 .trash로 이동 |
| `search_notes` | 제목으로 검색 |
| `full_text_search` | 내용 전문 검색 |
| `list_folders` | 모든 폴더 목록 |
| `get_tags` | 모든 태그 목록 |
| `get_vault_info` | 볼트 통계 정보 |

## 💬 사용 예시

설정 후 Claude에게 이렇게 요청할 수 있습니다:

- "내 최근 노트들 보여줘"
- "프로젝트 관련 노트 찾아줘"
- "'회의록'이라는 새 노트 만들어줘"
- "API 문서가 포함된 노트 검색해줘"
- "내 볼트에서 어떤 태그들을 쓰고 있어?"
- "TODO 노트에 새 항목 추가해줘"

## 🔒 보안

- **100% 로컬**: 모든 데이터가 내 컴퓨터에만 저장
- **API 키 불필요**: 외부 서비스 연결 없음
- **볼트 내부만**: 볼트 바깥 파일 접근 불가
- **안전한 삭제**: 삭제 시 .trash로 이동, 영구 삭제 아님

## 🐛 문제 해결

### "Vault not found" 오류
`OBSIDIAN_VAULT_PATH`가 올바른 Obsidian 볼트 폴더를 가리키는지 확인하세요.

### Claude Desktop에서 "Server disconnected"
1. `uv` 경로가 올바른지 확인 (전체 경로 사용)
2. obsidian-mcp 폴더 경로가 올바른지 확인
3. Claude Desktop 로그에서 오류 확인

### 권한 거부됨 (macOS)
터미널에 전체 디스크 접근 권한을 부여하세요:
시스템 환경설정 → 보안 및 개인 정보 보호 → 개인 정보 보호 → 전체 디스크 접근

## 📄 라이선스

MIT 라이선스 - [LICENSE](LICENSE) 참조

## 🤝 기여하기

기여를 환영합니다! [CONTRIBUTING.md](CONTRIBUTING.md)를 먼저 읽어주세요.

## 🙏 감사의 말

- [MCP (Model Context Protocol)](https://modelcontextprotocol.io/)로 제작
- Obsidian 커뮤니티에서 영감을 받음

